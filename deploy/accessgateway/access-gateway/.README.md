# deploy/accessgateway/access-gateway
* init.d - scripts to execute during different stages of the access-gateway container deployment
* overlay - files to overlay the access-gateway container installation

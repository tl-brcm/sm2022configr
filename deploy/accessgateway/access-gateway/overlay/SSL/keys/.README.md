# deploy/accessgateway/access-gateway/overlay/SSL/keys
* server.key -- the encrypted(password) private key file that goes with the ../certs/server.crt
* server.clear -- the non-encrypted version of server.key
